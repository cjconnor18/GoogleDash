class Calendar{
    constructor(id, summary, accessRole, conferenceProperties) {
        this.id = id;
        this.summary = summary;
        this.accessRole = accessRole;
    }
}

module.exports = Calendar;